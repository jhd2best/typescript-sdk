/**
 * @public
 */
export enum QueryOptionsFilterOperator {
    AND = "AND",
    OR = "OR",
}

/**
 * @public
 */
export enum QueryOptionsSortOrder {
    ASC = "ASC",
    DESC = "DESC",
}

/**
 * @public
 */
export enum QueryOptionsOperator{
    EQ = "EQ",
    LT = "LT",
    GT = "GT",
    NEQ = "NEQ",
}
